# Minimal VBP-shaped server (demo)

Not production-ready — shows route layout brokers expect. Run:

```bash
cd examples/minimal-node
node server.mjs
```

Then:

```bash
node ../../validator/cli.mjs http://127.0.0.1:3099/vbp/v1
```
