/**
 * Live v0 E2E: docs/SMOKE-TEST-ORCHESTRATOR.md (JWT + experiment + V0_API_KEY).
 */
import { assertEquals } from "https://deno.land/std@0.224.0/assert/mod.ts";

Deno.test("v0 e2e documented off-CI", () => {
  assertEquals(true, true);
});
